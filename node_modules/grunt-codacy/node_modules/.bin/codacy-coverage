#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../codacy-coverage/bin/codacy-coverage.js" "$@"
  ret=$?
else 
  node  "$basedir/../codacy-coverage/bin/codacy-coverage.js" "$@"
  ret=$?
fi
exit $ret
